#!/bin/bash

# custom rofi options
_rofi () {
    rofi -regex -tokenize -i -lines 30 -width 1500 -no-levenshtein-sort "$@"
}

# set root directory
noteroot="$HOME/Documents/todo"

# set browser
BROWSER="chromium"

# set editor
EDITOR="gvim -f"

# color of title/urls
title_color="#2d7ed8"
