#!/bin/bash

# custom rofi options
rofiopts="-line-margin 3 -width 700 -i -no-levenshtein-sort"

# set root directory
noteroot="$HOME/Documents/todo"

# set browser
BROWSER="chromium"

# set editor
EDITOR="gvim -f"
