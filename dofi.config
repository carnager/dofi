#!/bin/bash

# custom rofi options
_rofi () {
    rofi -i -lines 30 -width 1500 -no-levenshtein-sort "$@"
}

# set root directory
noteroot="$HOME/Documents/todo"

# set browser
BROWSER="chromium"

# set editor
EDITOR="gvim -f"
